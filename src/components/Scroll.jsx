import React from 'react'

const Scroll = ({ image }) => {
    return (
        <div>
            <a href="">
                <img src={image} alt="scroll item"/>
            </a>
        </div>
    )
}

export default Scroll